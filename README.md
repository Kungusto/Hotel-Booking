# 🏨 Hotel Booking API 🏨

**Hotel Booking API** — backend-сервис для управления бронированиями отелей. Поддерживает авторизацию, работу с отелями и номерами, оформление броней, кеширование, асинхронные задачи, контейнеризацию через Docker и автоматизированный CI/CD.

## 📋 Оглавление

- [🚀 Основной функционал](#основной-функционал)
- [✨ Плюсы и преимущества проекта](#плюсы-и-преимущества-проекта)
- [🛠️ Технологический стэк](#технологический-стэк)
- [⚙️ Настройка, запуск и тестирование](#настройка-запуск-и-тестирование)
- [🧪 Тестирование](#тестирование)
- [🏗️ Архитектура и паттерны](#архитектура-и-паттерны)
- [🚀 Запуск](#запуск)
- [🔁 CI/CD в Hotel Booking API](#cicd-в-hotel-booking-api-как-это-работает-и-зачем-это-нужно)
- [🚀 Быстрый старт](#быстрый-старт)
- [📚 Документация API](#документация-api)
- [🤝 Как внести вклад](#как-внести-вклад)

---

## 🚀 Основной функционал

- **Регистрация и аутентификация пользователей**  
  Безопасная система регистрации, входа и управления сессиями. Используются современные JWT-токены, обеспечивающие надежную защиту данных.

- **Гибкое бронирование номеров**  
  Пользователь может легко искать, бронировать и отменять бронирования номеров в режиме реального времени. Система учитывает занятость, фильтры по дате, количеству гостей, классу номера и другим параметрам.

- **Панель администратора**  
  Управление отелями, категориями номеров, тарифами и бронированиями. Встроенная система ролей с разграничением прав доступа для персонала и администраторов.

- **Интеграция с базой данных и кэшем**  
  Используется PostgreSQL для хранения данных и Redis для кэширования и ускорения отклика API.

- **Асинхронная обработка задач**  
  Celery и Redis обеспечивают фоновую отправку уведомлений, автоматическую очистку просроченных бронирований и другие задачи без задержки основного потока работы.

- **Миграции базы данных**  
  Alembic управляет схемой базы и легко позволяет обновлять структуру без боли и ручных вмешательств.

- **Документированное и покрытое тестами API**  
  Swagger/OpenAPI спецификация, подробные эндпоинты, примеры запросов и ответов. Есть отдельные .env-файлы для тестирования и запуска.

---

## ✨ Плюсы и преимущества проекта

- **Масштабируемость и архитектурные паттерны**  
  Проект легко масштабируется за счёт использования Docker и `docker-compose`, что позволяет запускать инфраструктуру как локально, так и в продакшене. CI/CD пайплайн (например, через GitLab) автоматизирует сборку, тестирование и деплой.  
  Архитектура построена на основе современных паттернов (подробней о каждом в разделе "Паттерны"):
  - Dependency Injection
  - Service Layer
  - Repository Pattern
  - DTO (Pydantic Schemas)
  - Model Layer (ORM)
  - Task/Job Pattern
  - Exception Handling
  - Configuration Pattern
  - API Router Pattern
  - Unit of Work

- **Безопасность**  
  Хранение чувствительных данных в .env, строгая валидация входных данных, поддержка HTTPS и шифрование JWT.

- **Простота развертывания**  
  Один docker-compose и несколько docker run — и проект работает! Всё, что нужно, — это стандартные образы PostgreSQL и Redis.

- **Высокая производительность**  
  Кэширование, асинхронные задачи, продуманная архитектура API — всё для максимальной скорости отклика даже при высокой нагрузке.

- **Гибкая настройка**  
  Множество параметров легко меняются через переменные окружения. Можно развернуть как локально, так и в облаке.

- **Удобство для разработчиков**  
  Структура кода проста для навигации, чистый requirements.txt, поддержка Alembic для миграций.

- **Современные технологии**  
  Python 3.12, Celery, Alembic, Docker — всё на острие прогресса.

---

## 🛠️ Технологический стэк

**Основные технологии:**

- Python 3.12.9
- FastAPI, SQLAlchemy, Alembic
- asyncpg (асинхронный драйвер PostgreSQL)
- Redis (кеширование, очереди) + aioredis, fastapi-cache2
- Celery (фоновая обработка задач)
- Passlib, bcrypt (хеширование паролей)
- Pydantic (валидация и сериализация)
- Pytest, pytest-asyncio (тестирование)

**Внешние сервисы и окружение:**

- PostgreSQL 16+ (основная база данных)
- Redis 7+ (кеш и очереди)
- Docker (контейнеризация)
- Nginx 1.28+ (удаленный сервер)

**Все зависимости указаны в:**  
[requirements.txt](https://github.com/Kungusto/Hotel-Booking/blob/main/requirements.txt)

---

## ⚙️ Настройка, запуск и тестирование

### Переменные окружения

Конфигурация переменных окружения находится в `src/config.py`. Основные параметры:

- `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME` — доступ к базе данных PostgreSQL
- `REDIS_HOST`, `REDIS_PORT` — настройки Redis
- `JWT_SECRET_KEY`, `JWT_ALGORITHM` — ключи для JWT и cookie
- Другие переменные можно посмотреть в `src/config.py`

**Пример файла `.env`:**

```plaintext
DB_HOST=localhost 
DB_PORT=5432
DB_USER=abcde
DB_PASSWORD=abcde
DB_NAME=bookings

REDIS_HOST=localhost
REDIS_PORT=6379

SECRET_KEY=your_secret_key
JWT_ALGORITHM=algorithm_name
```

---

## 🧪 Тестирование

### 📋 Общая информация

Проект **Hotel Booking API** сделан с упором на надежность и качество кода. Тесты охватывают все ключевые части — от бизнес-логики до интеграций. Разделение на unit и интеграционные тесты даёт гибкость, масштабируемость и простоту поддержки.

### 🏗 Структура тестов

```
tests/
├── unit_tests/            # Юнит-тесты (функции, классы)
├── integration_tests/     # Интеграционные тесты
│   ├── bookings/          # Бронирования
│   ├── hotels/            # Отели и номера
│   └── users/             # Пользователи и аутентификация
```

#### ⚙️ Unit-тесты (`tests/unit_tests`)

- Проверка отдельных функций и бизнес-логики изолированно.
- Быстрый запуск и локализация багов.
- Проверяют валидацию и ключевые вычисления.

#### 🔗 Интеграционные тесты (`tests/integration_tests`)

- Проверяют взаимодействие компонентов: API, БД, кэш, внешние сервисы.
- Покрывают реальные сценарии: бронирование, поиск, регистрация, ошибки.
- Отлавливают сложные кейсы, недоступные для юнит-тестов.

#### 📝 Особенности

- 🔒 Изолированное окружение с `.env-test`
- 🤖 Полная поддержка CI/CD (GitLab, GitHub Actions)
- 🐳 Запуск тестов в Docker-контейнерах для стабильности
- 🎯 Покрытие стандартных и граничных сценариев

---

## 🏗️ Архитектура и паттерны

### 🏛️ Архитектура

Проект построен по классической многоуровневой архитектуре с чётким разделением ответственности:

1. **API** — обработка HTTP-запросов и маршрутизация (FastAPI routers в `src/api/`)
2. **Service Layer** — бизнес-логика, обработка правил приложения (`src/services/`)
3. **Repository Layer** — абстракция работы с данными, CRUD-операции (`src/repositories/`)
4. **Model Layer** — ORM-модели, описание структуры базы данных (`src/models/`)
5. **Schema Layer** — Pydantic-схемы для валидации и сериализации (`src/schemas/`)
6. **Tasks** — фоновые и периодические задачи (Celery, `src/tasks/`)
7. **Utils и Exceptions** — вспомогательные функции и централизованная обработка ошибок

### 🧩 Паттерны и практики

1. **Dependency Injection (Внедрение зависимостей)**  
   - Используется механизм FastAPI `Depends`, который позволяет внедрять сервисы, репозитории, настройки и другие зависимости в обработчики и классы.  
   - **Преимущества:** Повышает тестируемость, легко подменять реализации, упрощает конфигурацию и управление зависимостями.

2. **Service Layer (Слой сервисов)**  
   - Вся бизнес-логика сосредоточена в сервисах (`src/services`), что отделяет её от деталей API и базы данных.  
   - **Преимущества:** Чёткое разделение кода, удобство поддержки и расширения функционала.

3. **Repository Pattern (Паттерн репозиториев)**  
   - Слой доступа к данным (`src/repositories`) инкапсулирует работу с базой через SQLAlchemy, реализуя CRUD-операции.  
   - **Преимущества:** Абстрагирует от конкретной СУБД, позволяет легко тестировать бизнес-логику без реальной базы.

4. **Data Transfer Object (DTO) / Pydantic Schemas**  
   - Pydantic-модели (`src/schemas`) валидируют и сериализуют данные, разделяя внутренние объекты и публичные API-форматы.  
   - **Преимущества:** Гарантия корректности данных, автоматическая генерация документации (OpenAPI).

5. **Model Layer (ORM-модели)**  
   - SQLAlchemy-модели в `src/models` описывают таблицы и связи в базе данных.  
   - **Преимущества:** Централизованное описание данных, удобство миграций и работы с БД.

6. **Task/Job Pattern (Фоновые задачи)**  
   - Celery используется для выполнения асинхронных и периодических задач (`src/tasks`), например, отправка email, обработка больших запросов.  
   - **Преимущества:** Отделение тяжёлых операций от основного API, масштабируемость.

7. **Exception Handling Pattern**  
   - Централизованная обработка ошибок в `src/exceptions` для формирования стандартных и понятных ответов на ошибки.  
   - **Преимущества:** Единый стиль обработки ошибок, улучшение UX.

8. **Configuration/Environment Pattern**  
   - Конфигурация вынесена в отдельный модуль `src/config.py`, параметры загружаются из переменных окружения (.env).  
   - **Преимущества:** Простота настройки под разные окружения, безопасное хранение секретов.

9. **API Router Pattern**  
   - Роуты разбиты по модулям в `src/api/` для структурированного и масштабируемого API.  
   - **Преимущества:** Чистая и понятная структура, легко добавлять новые разделы.

10. **Unit of Work (опционально)**  
    - Управление транзакциями через сессии SQLAlchemy для целостности данных при сложных операциях.  
    - **Преимущества:** Контроль и откат транзакций при ошибках.

---

## 🚀 Запуск

### 🐳 Docker

- Собрать образ:
```bash
docker build -t bookings_image .
```

- Запустить контейнеры для PostgreSQL, Redis, backend, worker, beat:
```bash
# сборка образа главного контейнера
-kernel:docker build -t bookings_image .

# создание сети для связи между контейнерами
docker network create myNetwork

# запуск базы данных (postgresql)
docker run --name booking_db -p 6432:5432 -e POSTGRES_USER=abcde -e POSTGRES_PASSWORD=abcde -e POSTGRES_DB=bookings --network=myNetwork --volume pg-booking-data:/var/lib/postgresql/data -d postgres:16

# запуск Redis
docker run --name booking_cache -p 7379:6379 --network=myNetwork -d redis:7.4

# запуск основного контейнера
docker run --name booking_back -p 7777:8000 --network=myNetwork bookings_image

# запуск celery-воркера
docker run --name celery_worker --network=myNetwork bookings_image celery --app=src.tasks.celery_app:celery_instance worker -l INFO --pool=solo

# запуск celery-beat
docker run --name celery_beat --network=myNetwork bookings_image celery --app=src.tasks.celery_app:celery_instance worker -l INFO -B --pool=solo

# создание томов для Nginx
--volume etc/letsencrypt:etc/letsencrypt --volume var/lib/letsencrypt
```

### 💻 Локально

- Установить зависимости:
```bash
pip install -r requirements.txt
```
- Применить миграции базы данных:
```bash
alembic upgrade head
```
- Запустить сервис с автообновлением:
```bash
uvicorn src.main:app --reload
```

---

## 🔁 CI/CD в Hotel Booking API: как это работает и зачем это нужно

В проекте **Hotel Booking API** реализован процесс CI/CD (Continuous Integration / Continuous Deployment) с помощью GitLab CI и Docker. Это значит, что каждая новая версия приложения автоматически собирается, тестируется и разворачивается — без ручных ошибок и лишней рутины.

### 🔧 Как реализовано в моём проекте?

1. **Автоматическая сборка:**  
   После каждого коммита или merge request GitLab CI запускает пайплайн:  
   - Приложение собирается в Docker-образ  
   - Все зависимости и настройки (например, переменные окружения) применяются автоматически  

2. **Автотесты:**  
   В пайплайн добавлен запуск тестов, чтобы убедиться, что новая версия не содержит ошибок и не ломает старый функционал.

3. **Автоматический деплой:**  
   После сборки приложение разворачивается в нужном окружении через Docker Compose. Всё происходит без участия разработчика — быстро и предсказуемо.

### ⚡ В чём польза CI/CD для проекта?

- **Стабильность:** Все изменения проходят одинаковый путь: тесты, сборка, деплой. Исключается человеческий фактор и “работает только у меня”.  
- **Быстрота:** Новые функции и фиксы становятся доступны пользователям за считанные минуты после слияния кода.  
- **Контроль качества:** Ошибки выявляются до публикации — если тесты не проходят, проект не будет автоматически развёрнут.  
- **Масштабируемость:** Легко добавить новые шаги: деплой в разные окружения, линтеры, публикацию контейнеров и др.

---

## 🚀 Быстрый старт

Чтобы быстро запустить проект, выполните следующие шаги:

1. **Склонируйте репозиторий**:
   ```bash
   git clone https://github.com/Kungusto/Hotel-Booking.git
   cd Hotel-Booking
   ```

2. **Настройте переменные окружения**:
   - Скопируйте файл `.env.example` в `.env` и заполните необходимые значения.

3. **Соберите и запустите Docker-контейнеры**:
   ```bash
   docker-compose up --build
   ```

4. **Получите доступ к API** по адресу `http://localhost:8000`.

---

## 📚 Документация API

API полностью задокументировано с использованием Swagger/OpenAPI. Интерактивная документация доступна по адресу `http://localhost:8000/docs` после запуска приложения.
